@model IEnumerable<WebApplication_brr_bmi1.Models.Viloyat>

@{
    ViewBag.Title = "Index";
}

<br />
<br />

<h1 style="text-align:center">Viloyat</h1>
<br />


<p class="btn btn-success">
    @Html.ActionLink("Viloyat qo'shish", "Create")
</p>
<br />
<br />
<table class="table table-striped">
    <tr>
        <th>
            <h3><b>Viloyat nomi</b> </h3>
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
               <h4> @Html.DisplayFor(modelItem => item.Viloyat_nomi)</h4>
            </td>
            <td>
                @Html.ActionLink("O'zgartirish", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Ma'lumotlar'", "Details", new { id = item.Id }) |
                @Html.ActionLink("O'chirish", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
